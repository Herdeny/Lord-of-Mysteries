# Project Mystery v0.8 仓库执行基线

> 来源：用户提供的 `Project_Mystery_Design_Doc_v0_8.md`
>
> 定位：把 30 万字全量 GDD/TDD 转换为当前 Forge 1.20.1 仓库可执行的增量约束。
>
> 冲突规则：仓库现有代码、命名空间、依赖和已通过测试优先。

## 1. 技术转换

| v0.8 文档 | 当前仓库执行规则 |
|---|---|
| Forge 47.3.x | 保持已验证的 Forge 47.4.20 |
| `pm:*` 示例 ID | 统一转换为 `lord_of_mysteries:*` |
| GeckoLib 4.x 已决策 | 保留 M5 正式接入，不在 M1/M2 强制增加运行依赖 |
| 1,100+ JSON | 分批迁移；CSV 主表与生成器先行 |
| `pm-bridge` | 独立运营仓库，不进入 Mod 本体 |
| 全量设计数字 | 长期目标，不得作为当前完成度 |

## 2. 当前里程碑

M1 仍是当前门禁：普通人生存流程需要连续完成占卜家序列 9-7，并用 `/pm trial`
记录一小时、营地、晋升、三次特殊狩猎、两次有效扮演和风险峰值。

M2 只进入预研：

- 22 途径数据目录与原创化映射。
- 观众/猎人序列 9-7 已形成可玩预研纵切。
- 偷盗者/学徒序列 9-7 已形成可玩代码预研，含魔药、能力、扮演、材料与分层按键。
- 雾都生活已从文书入口推进到轻量前哨、委托板、便士账本、寻猫引导和失踪调查小队序章。
- 完整镇区、NPC、链一后半段、多人矩阵和 schema 冻结仍待实现。

## 3. 数据生产决策 D9

人工维护：

- `docs/pathways_master.csv`
- `docs/recipes_master.csv`

自动生成：

- `src/generated/resources/data/lord_of_mysteries/pathway_catalog/*.json`
- `src/generated/resources/data/lord_of_mysteries/recipes/*.json`
- `src/generated/resources/data/lord_of_mysteries/project_mystery/generated_manifest.json`

命令：

```bash
python scripts/gen_datapack.py
python scripts/gen_datapack.py --check
./gradlew clean build
```

Gradle 的 `processResources` 和 `check` 已接入生成器。后续物品、能力、任务和事件数据必须优先
扩展 CSV/schema，而不是继续批量手写同构 JSON。

## 4. M1 指引与验收

- 原调查员手稿升级显示为“神秘学手账”，保留稳定物品 ID。
- `/pm handbook` 显示九章目录，`/pm handbook <1-9>` 阅读章节。
- 安全、成为非凡者、风险和途径章节默认开放。
- 扮演/仪式随成为非凡者开放，占卜随占卜家开放，生计随 M2 传闻开放。
- 灰雾章节在 M4 邀请知识出现前保持锁定。
- `/pm trial` 机器目标从 5 项增至 6 项，新增至少两次正收益扮演事件。

## 5. M2 首批内容

| 内容 | 当前状态 |
|---|---|
| 22 途径 CSV 目录 | 已生成并进入构建校验 |
| 偷盗者、学徒 | 序列 9-7 魔药、能力、十八项扮演、知识副本、空间与解密循环已进入可玩预研 |
| 观众、猎人 | 序列 9-7 预研可玩，含序列 7 魔药、能力、扮演与独立失控体 |
| 空白手稿、神秘墨水、报纸 | 已注册并由 CSV 生成配方 |
| 配方残页 | 营地战利品；右键依次解锁四条 M2 调查知识 |
| 灵墨知识副本 | 学徒可复制已知稳定知识并交给其他玩家学习 |
| 雾都镇区/委托/链一 | 前哨、两种委托和七步序章可玩；完整镇区与后半链待实现 |
| 五途径序列 9-7 | 代码纵切已齐，待多人、数据契约和内容门禁验收 |

## 6. 后续生产顺序

1. 用真实一小时记录关闭 M1 阻断项。
2. ~~为偷盗者、学徒补齐序列 9 的魔药、能力、扮演和失控体。~~
3. ~~补齐观众、猎人序列 7，再扩展五途径一致性测试。~~
4. ~~补齐偷盗者、学徒序列 8-7。~~
5. ~~实现委托数据 schema、寻猫引导和“失踪调查小队”可玩序章。~~
6. 在轻量前哨基础上增加报社/教堂/事务所 NPC、记者救援、护送和夜袭。
7. 冻结 M2 schema、网络协议和原创化词表。

## 7. 内容与版权边界

- 不提交小说原文、官方插画、官方 Logo、提取模型、音乐或字体。
- 公开名称全部经过语言文件；内部 ID 使用稳定英文代号。
- 原创代码使用 GPL-3.0-or-later；原创文档与美术遵循仓库资产许可。
- v0.8 原始文档是设计输入，本执行版只保留当前仓库能够维护和验证的工程约束。
