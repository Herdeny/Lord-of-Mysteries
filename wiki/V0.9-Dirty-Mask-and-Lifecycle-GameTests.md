# v0.9 状态分区与生命周期 GameTest

> 当前版本：0.9.2-1.20.1 · Capability schema 16 · 网络协议 8

## 已完成

- Core、Knowledge、Social、Endgame 四区 dirty mask；公共字段和集合原地修改会自动置脏。
- 登录、重生、维度切换立即同步核心摘要；每秒检查变化，每 5 秒强制校正。
- 独立摘要包不会打开状态 GUI，也不发送完整 `knownKnowledge`。
- 客户端只读状态缓存会在退出服务器时清空。
- 网络协议 8 使用 12 个固定、连续、唯一消息 ID。
- 202 项 JUnit 与 3 项真实 Forge GameTest 进入 Build CI。

## GameTest 证据

1. `ServerPlayer` Capability Clone 保留途径、能力、知识、经济和 M1 试炼状态。
2. schema 15 NBT 经 Provider/DataFix 往返升级到 schema 16，并保留迁移备份与历史。
3. future schema 原始字段进入 `orphaned_entries`，不会被旧版本静默覆盖。

```bash
./gradlew clean build
./gradlew runGameTestServer
python scripts/run_server_smoke.py --timeout 180
```

有效结果必须显示 `3 tests are now running` 与 `All 3 required tests passed`；`0 tests` 不算通过。

## 仍待完成

- 内容图从 71 节点扩至至少 100。
- 自动化服务端重启与降级回滚演练。
- 真人客户端死亡/重生、跨维度、断线和多人长时验收。
- Knowledge、Social、Endgame 专用增量包与 UI 消费。

完整架构和矩阵见仓库 `docs/V0.9_DIRTY_MASK_AND_LIFECYCLE_GAMETEST.md`。
