# 秘偶大师持久编队

> 适用版本：`0.9.27-1.20.1` · 状态：`code_ready`

## 快速使用

1. 晋升占卜家途径序列 5 秘偶大师。
2. 把普通敌对生物削弱到 20% 生命以下；目标最大生命不能超过 80。
3. 对准 14 格内目标，潜行按 `U`，消耗 60 灵性并进入 120 秒创建冷却。
4. 输入 `/pm marionette` 查看最多三个持久槽位。
5. 用 `/pm marionette recall` 召回已加载且同维度的秘偶。
6. 用 `/pm marionette release <1..3>` 或 `release all` 主动解除。

普通按 `U` 仍是原有提线束缚；只有潜行 `U` 尝试创建秘偶。

## 安全规则

- 只接受敌对、非玩家、非 Boss、未被其他玩家拥有的濒死目标。
- 所有检查先于资源扣除；失败不扣灵性、不开始冷却、不修改目标。
- 编队 UUID 与实体门主 UUID 双重持久化，Capability schema 27 提供旧档迁移和坏值修复。
- 秘偶同维跟随，并协助攻击门主最近攻击者或伤害门主的非玩家目标。
- 秘偶不能伤害玩家或任何玩家的秘偶；门主不能误伤自己的秘偶，其他普通生物仍可伤害秘偶。
- 召回只处理已加载实体，不强制加载区块、不跨维传送，并要求坚实无碰撞落点。
- 门主离线或跨维时秘偶停机；离线期间死亡留下的旧槽位可用 `release` 清理。

## 当前边界

已完成三槽持久编队、跟随、战斗协助、玩家/友军保护、安全召回、显式解除、schema 27 迁移和
真实 Forge GameTest。收纳卷轴、跨维指挥、离线全世界自动清账、高级战术 AI、完整特性守恒
转换与专属美术仍未完成。

完整数值、失败矩阵、存档格式和验证证据见
[`docs/M3_MARIONETTE_SQUAD.md`](https://github.com/Herdeny/Lord-of-Mysteries/blob/main/docs/M3_MARIONETTE_SQUAD.md)。
